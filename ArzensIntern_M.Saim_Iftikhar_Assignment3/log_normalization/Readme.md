# Security Log Normalizer

## Overview
Normalizes mixed security logs (firewall, auth, DNS, file access) into CSV and JSON with consistent schema.

## Requirements
- Python 3.9+
- No external dependencies (uses built‑in modules).

## Installation
```bash
git clone <your-repo>
cd <project-dir>